# =========================================
# 몽글픽 추천 서비스 의존성
# =========================================

# 웹 프레임워크
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ORM / 비동기 DB
sqlalchemy[asyncio]>=2.0.0
aiomysql>=0.2.0

# Redis 캐시
redis[hiredis]>=5.0.0

# JWT 인증 (Spring Boot 토큰 검증)
pyjwt>=2.8.0

# 데이터 검증 / 설정
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP 클라이언트
httpx>=0.27.0

# 환경변수 로드
python-dotenv>=1.0.0

# 백그라운드 스케줄러 (좋아요 write-behind flush, 2026-04-07)
# Redis dirty 큐를 주기적으로 드레인하여 MySQL likes 테이블에 배치 반영한다.
apscheduler>=3.10.4

# Prometheus 메트릭 노출 (/metrics) — VM3 Prometheus 스크레이프 대상
prometheus-fastapi-instrumentator>=7.0.0
